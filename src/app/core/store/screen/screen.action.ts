import { createAction, props } from '@ngrx/store';

export const screenType = {
  isMobile: '[screen] is mobile',
  isDesktop: '[screen] is desktop',
};

// export const isMobile = createAction(
//   screenType.isMobile
// );

// export const isDesktop = createAction(
//   screenType.isDesktop
// );
